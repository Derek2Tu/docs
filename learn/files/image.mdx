<Info>The max file size is 10 MB but you can be increased upon request.</Info>

We use Google’s [Multimodal Embedding model](https://cloud.google.com/vertex-ai/docs/generative-ai/image/overview) on Vertex AI for image embeddings. This allows users to search through uploaded images either based on the description of the image content or the text within an image (similar to OCR).

Carbon supports the following image file formats:

| File Formats |
| -------- | 
| `JPG`     | 
| `PNG`     | 