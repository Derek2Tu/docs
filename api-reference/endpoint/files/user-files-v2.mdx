---
title: "View User Files"
description: "When filtering by tag, tag keys must be strings and tag values must be string or list<string>. All tags are combined with AND logic. For tags filters with list<string> values, all elements of the list are combined with AND logic as well."
openapi: post /user_files_v2
---